# Generated by build.js — edit there, not here.

[build]
  publish = "dist"
  # Explicit, so the host cannot fall back to `npm start` and launch the
  # dev server, which would hang the deploy until it timed out.
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"
